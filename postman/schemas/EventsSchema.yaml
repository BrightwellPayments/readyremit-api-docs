openapi: 3.0.1
info:
  title: Events
  description: Import from "func-rr-events-dev-eastus-001" Function App
  version: '1.0'
servers:
  - url: https://api.readyremit.com/events
paths:
  /:
    post:
      summary: Events
      operationId: post-logevent
      responses:
        '200':
          description:
components:
  securitySchemes:
    apiKeyHeader:
      type: apiKey
      name: Ocp-Apim-Subscription-Key
      in: header
    apiKeyQuery:
      type: apiKey
      name: subscription-key
      in: query
security:
  - apiKeyHeader: []
  - apiKeyQuery: []
